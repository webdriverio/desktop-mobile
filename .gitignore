# Phase -1 spike (Dioxus automation API check) — throwaway code, do not commit
/spike/

# Rust build artefacts (Cargo.lock IS tracked -- committed for reproducible builds,
# consistent with pnpm-lock.yaml; only build output is ignored)
packages/*/target/

# Logs
logs
logs-*
*.log
npm-debug.log*
lerna-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory
coverage
*.lcov
.nyc_output

# Dependency directories
node_modules/
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# dotenv environment variables file
.env
.env.test
.env.local

# Build outputs
dist
dist-js
dist-electron
out
build

# Turbo files
.turbo

# OS files
.DS_Store

# IDE files
.vscode
.idea

# Claude Code tooling
.claude/worktrees/

# Test coverage
coverage

# Vitest
.vitest

# Tauri build files
# `src-tauri/target/*` is the v1 single-crate layout; the workspace fixtures
# now keep `target/` at the workspace root, so ignore both shapes.
**/src-tauri/target/*
**/autogenerated/**/*
fixtures/package-tests/tauri-app/target/
fixtures/e2e-apps/tauri/target/

# Dioxus build files (same v1 single-crate vs workspace-root distinction as Tauri)
**/src-dioxus/target/*
fixtures/package-tests/dioxus-app/target/
fixtures/e2e-apps/dioxus/target/

# Bundler fixtures (auto-generated rollup config)
fixtures/build-*/**/rollup.config.js

# MCP config files
.mcp.json

