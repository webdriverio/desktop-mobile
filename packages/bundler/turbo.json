{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "inputs": [
        "src/**",
        "tsconfig.json",
        "tsconfig.*.json",
        "wdio-bundler.config.ts",
        "rollup.config.ts",
        "package.json",
        "$TURBO_ROOT$/pnpm-lock.yaml"
      ]
    }
  }
}
