// browser.reactNative.triggerDeeplink — Appium `mobile: deepLink` (+ Android
// `am start` fallback). Shared in @wdio/native-mobile-core; RN uses the default
// cap keys (appium:appPackage / appium:bundleId).

export { triggerDeeplink } from '@wdio/native-mobile-core';
