// browser.reactNative.switchWindow / listWindows — Appium context switching
// (NATIVE_APP ↔ WEBVIEW_*). The logic is shared in @wdio/native-mobile-core.

export { listWindows, switchWindow } from '@wdio/native-mobile-core';
