// Public types for @wdio/flutter-service re-exported from @wdio/native-types
// (mirrors the sibling services — config-time types live in native-types so the
// ambient module augmentation reaches consumers).

export type {
  FlutterCapabilities,
  FlutterMock,
  FlutterMockInstance,
  FlutterResult,
  FlutterServiceAPI,
  FlutterServiceCapabilities,
  FlutterServiceCapabilitiesType,
  FlutterServiceGlobalOptions,
  FlutterServiceOptions,
  WdioFlutterConfig,
} from '@wdio/native-types';
